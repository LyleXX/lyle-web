import React, { memo } from 'react'

const Blog = memo(() => {
  return (
    <div className="mx-auto w-screen-pc text-t1">
      Blog
      <button>123</button>
    </div>
  )
})

export default Blog
